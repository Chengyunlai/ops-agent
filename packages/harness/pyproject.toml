[project]
name = "ops_agent_harness"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "kubernetes>=36.0.3",
    "urllib3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ops_agent"]
